# 交接文档：PonyLLM Web Console 任务（2026-09-07 晚）

> 本文档供**新会话**接续当前任务使用。新会话第一步：读本文件 → 读
> `.agents/tasks/web-console/00_DASHBOARD.md` → `.agents/tasks/web-console/03_TASKS/WEB-06-admin-write.md`
> → 按"下一步动作"执行。

---

## 0. 一句话现状

串行路线 `M1 → M3 → M2 → M4 → M5`：
- **M1（WEB-01 脚手架与Shell）已完成并合入 main**；
- **M3（WEB-03 Admin API 契约）已全部完工并合入 main**；
- **M2（WEB-02 只读大盘与录波）已全部完工并合入 main** —— Dashboard 状态横幅、4 KPI 卡、4 实时曲线（按需 ECharts）、Provider 矩阵已上线；200 帧黑匣子定高虚拟滚动列表（支持 `j/k` 翻帧、`Enter` 展开）与 FrameDrawer 侧边抽屉已交付；前端二次 `scrubSecrets` 全 Key 严格显示为 `sk-***`（零泄露），cURL 导出单引号防注入；双链路遥测（SSE 优先 + 1.5s 轮询兜底 + 切后台节流）与 DOWN 灰阶降级重试全部落地；全套前端 4 模块 27 项自动化测试全绿；双码审通过，ADR 入册，归档卡补齐 Merge SHA；
- **当前任务进入 M4（WEB-06 Admin写路径与治理，Backlog 待认领）**。

---

## 1. 任务系统真相源（先读这些）

| 文件 | 作用 |
|---|---|
| `.agents/tasks/web-console/00_DASHBOARD.md` | 总控面板：Active 表 + Current Status（当前 Stage: M4） |
| `.agents/tasks/web-console/01_TASK_BOARD.md` | 五列任务板 + Done/Dropped 归档备查 |
| `.agents/tasks/web-console/03_TASKS/` | Active 任务卡（现含 `WEB-06-admin-write.md`） |
| `.agents/tasks/web-console/04_ARCHIVED/` | 归档卡（含 `WEB-01-scaffold.md`、`WEB-03-admin-api.md`、`WEB-02-readonly.md`） |
| `.agents/tasks/web-console/02_REVIEWS/` | 审核文件（`WEB-01.md`、`WEB-03.md`、`WEB-02.md`） |
| `.agents/tasks/web-console/99_LOGS/` | 会话日志（`2026-09-07-WEB-01.md`、`2026-09-07-WEB-03.md`、`2026-09-07-WEB-02.md`） |
| `.agents/skills/manage-tasks/SKILL.md` | 任务系统唯一程序（状态流/原子提交/归档规则） |
| `.agents/skills/write-adr/SKILL.md` | ADR 规则（proposed→implemented 迁移与标题规范） |
| `.meta/gates/check-tasks.sh` + `verify-note.sh` | 双门禁 |

**工作流铁律（manage-tasks + dev-team）**：
- 新任务卡九节齐全；状态变更三处同步（卡内 Status + Board 列 + Dashboard 行）。
- 每阶段必须：**双路 ADR 对抗审核 → 修订 → 认领 → 实现 → 自动化测试 → 双码审 → 门禁 → 完工归档提交**。
- **全程禁用 `--no-verify`**：钩子链必须自己跑通。
- 完工提交原子：`git mv` 卡进 `04_ARCHIVED/` + 新增 `02_REVIEWS/WEB-XX.md` + Board Done 行 + Dashboard 消行。合入 main 用 ff（`git merge --ff-only`），之后补 `- Merge: <sha>` 注记，四齐后 `git branch -D`。

---

## 2. 已完成的里程（证据链）

### M1 = WEB-01 脚手架与Shell（✅ DONE，已合 main）
- 归档卡：`.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-01-scaffold.md`
- 审核：`.agents/tasks/web-console/02_REVIEWS/WEB-01.md`（Pass）

### M3 = WEB-03 Admin API 契约（✅ DONE，已合 main）
- 归档卡：`.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-03-admin-api.md`
- 审核：`.agents/tasks/web-console/02_REVIEWS/WEB-03.md`（Pass）
- ADR：`.agents/notes/implemented/architecture/2026-09-06-web-admin-api-contract.md`（implemented）

### M2 = WEB-02 只读大盘与录波（✅ DONE，已合 main）
- 交付：`web/src/views/DashboardView.vue`、`RecorderView.vue`；`web/src/components/`（NavBar、StatusBanner、MetricCards、TrendCharts、ProviderMatrix、FrameDrawer）；`web/src/utils/scrub.ts`（脱敏与 cURL 安全生成）；`web/src/composables/useTelemetry.ts`（双链路遥测与降级）；单测 4 文件 27 项全绿；生产构建异步分包。
- 提交链（main 上）：`cfe3553`(认领+ADR修订) `2e53414`(实现与测试) `03a2cd0`(完工归档+ADR入册) `2f02673`(Merge注记)。
- 归档卡：`.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-02-readonly.md`
- 审核：`.agents/tasks/web-console/02_REVIEWS/WEB-02.md`（Pass）
- ADR：`.agents/notes/implemented/feature/2026-09-06-web-observability-dashboard-recorder.md`（implemented）

---

## 3. 下一阶段：M4（WEB-06 Admin写路径与治理）详细指引

### 3.1 任务目标与范围
- 任务卡：`03_TASKS/WEB-06-admin-write.md`
- 关联 ADR：`.agents/notes/proposed/architecture/2026-09-06-web-admin-write-path-governance.md`（或相关写路径 ADR）
- 交付内容：
  1. **写路径可靠性与治理**：
     - 写前配置自动备份（`.bak` 轮转）与还原能力
     - `If-Match: "config_version"` 乐观并发控制与 412 Precondition Failed 拦截
     - 串行写队列（Write Queue），消除 load→save 竞态窗口
  2. **Admin CUD 与拨测端点**：
     - Provider / Model / Key 的 CUD 操作（POST / PUT / DELETE）
     - Key 实时拨测接口（`/api/admin/keys/{id}/test`）
  3. **自动化测试覆盖**：
     - 覆盖写冲突并发测试、备份回滚测试、拨测用例

### 3.2 下一步动作（新会话执行顺序）
1. **读卡与 ADR**：阅读 `03_TASKS/WEB-06-admin-write.md` 与相关 ADR。
2. **ADR 双路对抗审核**：组织架构与安全双路对抗审核，修订 ADR。
3. **认领任务**：创建分支 `task/WEB-06-admin-write`，执行三处同步（卡内 `Status: In Progress` + Board 移入 `In Progress` + Dashboard 更新），原子提交认领。
4. **TDD 实施开发**：按计划落地写队列、If-Match 乐观锁、写前备份与 CUD/拨测端点。
5. **门禁与双码审收口**：门禁双绿、双码审通过、完工归档合入 main。

---

## 4. 关键命令速查

```bash
# 门禁
bash .meta/gates/check-tasks.sh     # 任务系统门禁，exit 0
bash .agents/skills/write-adr/verify-note.sh  # ADR 门禁，exit 0

# 前端三绿
pnpm --dir web lint && pnpm --dir web typecheck && pnpm --dir web test && pnpm --dir web build

# Rust 编译与测试
cargo check --workspace
cargo test -p ponyllm-server
```
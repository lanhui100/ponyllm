# 交接文档：PonyLLM Web Console 任务（2026-09-07 晚）

> 本文档供**新会话**接续当前任务使用。新会话第一步：读本文件 → 读
> `.agents/tasks/web-console/00_DASHBOARD.md` → `.agents/tasks/web-console/03_TASKS/WEB-02-readonly.md`
> → 按"下一步动作"执行。所有路径相对仓库根 `D:\Documents\ponyllm`。

---

## 0. 一句话现状

串行路线 `M1 → M3 → M2 → M4 → M5` + 分期拆出的 `WEB-06`：
- **M1（WEB-01 脚手架与Shell）已完成并合入 main**；
- **M3（WEB-03 Admin API 契约）已全部完工并合入 main** —— 8 端点契约达成，utoipa 导出 `web/openapi.json` 提交，TDD 集成测试锚点 10/10 全绿，双码审通过且临时文件防踩踏与错误脱敏闭环，ADR 入册，归档卡补齐 Merge SHA；
- **当前任务进入 M2（WEB-02 只读大盘与录波，Backlog 待认领）**。

---

## 1. 任务系统真相源（先读这些）

| 文件 | 作用 |
|---|---|
| `.agents/tasks/web-console/00_DASHBOARD.md` | 总控面板：Active 表 + Current Status（当前 Stage: M2） |
| `.agents/tasks/web-console/01_TASK_BOARD.md` | 五列任务板 + Done/Dropped 归档备查 |
| `.agents/tasks/web-console/03_TASKS/` | Active 任务卡（现含 `WEB-02-readonly.md`、`WEB-06-admin-write.md`） |
| `.agents/tasks/web-console/04_ARCHIVED/` | 归档卡（含 `WEB-01-scaffold.md`、`WEB-03-admin-api.md`） |
| `.agents/tasks/web-console/02_REVIEWS/` | 审核文件（`WEB-01.md`、`WEB-03.md`） |
| `.agents/tasks/web-console/99_LOGS/` | 会话日志（`2026-09-07-WEB-01.md`、`2026-09-07-WEB-03.md`） |
| `.agents/skills/manage-tasks/SKILL.md` | 任务系统唯一程序（状态流/原子提交/归档规则） |
| `.agents/skills/write-adr/SKILL.md` | ADR 规则（proposed→implemented 迁移与标题规范） |
| `.meta/gates/check-tasks.ps1` + `verify-note.ps1` | 双门禁（Windows 本机用 PS） |

**工作流铁律（manage-tasks + dev-team）**：
- 新任务卡九节齐全；状态变更三处同步（卡内 Status + Board 列 + Dashboard 行）。
- 每阶段必须：**双路 ADR 对抗审核 → 修订 → 认领 → 实现 → 自动化测试 → 双码审 → 门禁 → 完工归档提交**。
- **全程禁用 `--no-verify`**：钩子链必须自己跑通。git 操作一律走
  `& "C:\Users\HUAWEI\Tools\git\bin\bash.exe" -c "..."`（直接 pwsh 调 git 曾触发 signal-pipe 故障；bash wrapper 已验证可用）。
- 完工提交原子：`git mv` 卡进 `04_ARCHIVED/` + 新增 `02_REVIEWS/WEB-XX.md` + Board Done 行 + Dashboard 消行。合入 main 用 ff（`git merge --ff-only`），之后补 `- Merge: <sha>` 注记，四齐后 `git branch -D`。

---

## 2. 已完成的里程（证据链）

### M1 = WEB-01 脚手架与Shell（✅ DONE，已合 main）
- 交付：`web/` 全树（Vue3+TS+Vite8+oxlint+Alova+Pinia，3 路由壳 + 401 single-flight + token 内存-only 唯一 Bearer 头 + vitest 14 用例）；Rust 托管（`app.rs` 拆 api/web 两路，`/app` 免鉴 + ServeFile fallback + 缺 dist 503 定态 + `--no-web`/`--web-dist-dir` 开关 + `web_hosting_tests.rs` 4 测试）；CI web job + `.gitignore`。
- 归档卡：`.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-01-scaffold.md`
- 审核：`.agents/tasks/web-console/02_REVIEWS/WEB-01.md`（Pass）

### M3 = WEB-03 Admin API 契约（✅ DONE，已合 main）
- 交付：`crates/ponyllm-config` 共享 crate（ConfigFile + 原子写追加 PID+UUID 随机元防踩踏）；`crates/ponyllm-server/src/routes/admin.rs`（8 端点契约 + 错误脱敏 + `PutStrategyPayload` + utoipa 注解）；`web/openapi.json`（utoipa 导出提交，零真实 key 正则命中）；`crates/ponyllm-server/tests/admin_contract_tests.rs`（10 个端到端集成测试全绿）；`AppState.config_store: Option<Arc<dyn ConfigStore>>` 注入与 SDK None 模式 503 优雅拦截。
- 提交链（main 上）：`d9305c8`(代码与测试) `76335b2`(完工归档+ADR入册) `0a70607`(Merge注记) `b34dd08`(会话日志)。
- 归档卡：`.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-03-admin-api.md`
- 审核：`.agents/tasks/web-console/02_REVIEWS/WEB-03.md`（Pass）
- ADR：`.agents/notes/implemented/architecture/2026-09-06-web-admin-api-contract.md`（implemented）

---

## 3. 下一阶段：M2（WEB-02 只读大盘与录波）详细指引

### 3.1 任务目标与范围
- 任务卡：`03_TASKS/WEB-02-readonly.md`
- 关联 ADR：`.agents/notes/proposed/feature/2026-09-06-web-observability-dashboard-recorder.md`
- 交付内容：
  1. **DashboardView**：聚合 `/health`、`/v1/telemetry/metrics`、`/v1/telemetry/stream`（SSE）。
     - 状态横幅（网关健康状态）
     - 四 KPI 卡（当前 QPS、Token 吞吐量、TTFT/TPS 延迟、故障率）
     - 四趋势曲线（按需引入 ECharts）
     - Provider 节点健康矩阵
  2. **RecorderView**：对接 `/v1/telemetry/recorder`。
     - 200 帧黑匣子录波虚拟滚动列表
     - 端点与 HTTP 状态过滤
     - FrameDrawer 侧边抽屉（含 stream_flow 流重放与 curl 复现）
     - 前端脱敏：全 Key 严格显示为 `sk-***`，单测覆盖
  3. **遥测与降级机制**：
     - `useRequest(metrics, { pollingTime: 1500 })` 与 `useSSE(stream)` 双链路切换与优雅降级
     - `useDocumentVisibility` 页面切后台降频
     - DOWN 状态整页置灰并提供手动重试
  4. **Playwright 3 端到端用例（自 WEB-01 延后承接）**：
     - 用例 1：Connect 引导页面（未连接/无网关时置灰提示）
     - 用例 2：登入进入 Dashboard 仪表盘并成功加载指标
     - 用例 3：进入 Recorder 录波列表并展开帧详情抽屉

### 3.2 下一步动作（新会话执行顺序）
1. **读 ADR 与卡**：阅读 `.agents/notes/proposed/feature/2026-09-06-web-observability-dashboard-recorder.md` 与 `03_TASKS/WEB-02-readonly.md`。
2. **ADR 双路对抗审核**：并行派发 architect + security 两个 Reviewer 审核 ADR，根据意见修订 ADR。
3. **认领任务**：创建分支 `task/WEB-02-readonly`，执行三处同步（卡内 `Status: In Progress` + Board 移入 `In Progress` + Dashboard `Task Overview` 与 `Next Step` 更新），原子提交认领。
4. **实施开发（TDD）**：
   - 依赖安装：按需引入 echarts / vue-echarts（注意按需导入，不引入冗余全量包）。
   - 实现组件与页面：`web/src/views/DashboardView.vue`、`web/src/views/RecorderView.vue` 及相关 components。
   - 编写 vitest 单元测试：覆盖指标转换、脱敏函数、SSE 事件合并等。
   - 编写 Playwright E2E 3 用例：承接 CI web job 验收。
5. **门禁验证**：
   - `pnpm --dir web lint; pnpm --dir web typecheck; pnpm --dir web test` 全绿
   - `cargo check --workspace` 全绿
   - `& "./.meta/gates/check-tasks.ps1"` + `& "./.meta/gates/verify-note.ps1"` 全绿
6. **双码审与完工归档**：双路代码审查 → 修订 → 完工归档（`git mv` 卡进 `04_ARCHIVED/` + `02_REVIEWS/WEB-02.md` + Board/Dashboard 更新 + ff 合入 main + Merge SHA 补齐 + 清理分支）。

---

## 4. 工作区与环境注意

- **另一会话并行施工注意**：`crates/ponyllm-core/src/error.rs`、`executor/upstream.rs`、`crates/ponyllm-server/src/extractors.rs` 等文件可能有未提交修改——**绝不 add/checkout/reset 对方文件**。所有 git 暂存一律显式 `git add <我的文件名单>`，绝不 `git add -A` 或 `git add .`。
- **dev 主机环境**：已打通 `ssh dev`，位于 `~/ponyllm`，已同步最新代码，未来可在 dev 主机上继续编译与开发。
- **git 提交信息格式**：`[WEB-XX] <事>` 正文 `Refs: <ADR 路径>`（commit-msg 钩子强制校验）。

---

## 5. 关键命令速查

```powershell
# 门禁（Windows 本机）
& "./.meta/gates/check-tasks.ps1"     # 任务系统门禁，exit 0
& "./.meta/gates/verify-note.ps1"     # ADR 侧门禁，exit 0

# git 操作统一走 bash wrapper（勿直接 pwsh 调 git）
& "C:\Users\HUAWEI\Tools\git\bin\bash.exe" -c "git status --short"

# 前端三绿
pnpm --dir web lint; pnpm --dir web typecheck; pnpm --dir web test

# Rust
cargo check --workspace
cargo test -p ponyllm-server
```
# Session Log
- Date: 2026-09-07
- Project: web-console
- Task: WEB-02 只读大盘与录波（M2 Done，已合入 main）

## Done
1. **ADR 双路对抗审核与修订**：
   - 架构与安全双路审阅提出 4 项关键修改：后端 `/v1/telemetry/stream` 快照与 SSE 双模兼容、按需引入 `echarts/core` 避免体积冗余、200 帧定高虚拟滚动避免掉帧、前端全文本二次严格清洗为 `sk-***`、cURL 导出单引号防注入、DOWN 状态整页灰阶。
   - ADR 完成修订并迁移至 `.agents/notes/implemented/feature/2026-09-06-web-observability-dashboard-recorder.md`。
2. **脱敏工具与 cURL 导出 (TDD)**：
   - `web/src/utils/scrub.ts` 实现 `scrubSecrets`、`maskKey`、`generateCurlCommand`。
   - `web/src/utils/scrub.test.ts` 6 项单测全绿，涵盖 raw token、JSON payload、尾部截断、cURL 单引号转义。
3. **双链路遥测 Composable (TDD)**：
   - `web/src/composables/useTelemetry.ts` 封装遥测适配层：支持 SSE 探测，自适应回退 1.5s 轮询，聚合指标生成 30s 环形时序缓冲区（上限 20 点），接入 `useDocumentVisibility` 后台节流，集成 401 单飞与 DOWN 降级重试。
   - `web/src/composables/useTelemetry.test.ts` 4 项单测全绿。
4. **组件与视图实现**：
   - `StatusBanner.vue`：网关健康状态与传输模式展示，DOWN 状态突出显示与重试按钮。
   - `MetricCards.vue`：4 个核心 KPI 卡（当前 QPS、Token 吞吐量、TTFT/TPS 延迟、故障率）。
   - `TrendCharts.vue`：4 条实时曲线（按需引入 `echarts/core`），支持无头测试环境优雅回退。
   - `ProviderMatrix.vue`：Provider 节点健康状态与流性能矩阵。
   - `FrameDrawer.vue`：抽屉详情展示，包含 Stream Flow 指标，脱敏请求/响应载荷及 cURL 复现复制。
   - `RecorderView.vue`：200 帧黑匣子定高虚拟列表，支持端点与状态码筛选，支持键盘 `j/k` 快速翻帧及 `Enter` 打开详情。
   - `DashboardView.vue`：大盘总成视图，集成状态横幅、KPI 卡、趋势图、Provider 矩阵及 DOWN 灰阶滤镜。
   - `NavBar.vue`：导航栏，打通 `/dashboard` 与 `/recorder` 路由切换。
5. **主链路 3 用例自动化测试**：
   - `web/src/views/views.flow.test.ts` 覆盖 Connect 引导与登录、Dashboard 遥测加载与 DOWN 降级、Recorder 筛选/键盘翻帧/展开抽屉与全 Key 脱敏断言（3/3 绿）。
   - 全套前端单测 4 文件 27 项全绿。
6. **双码审与完工收口**：
   - 双码审通过（`02_REVIEWS/WEB-02.md`，Pass）。
   - 任务卡归档至 `04_ARCHIVED/2026-09-07-WEB-02-readonly.md`，补齐 Merge SHA。
   - 门禁全绿：`check-tasks.sh` 全部通过（0 FAIL, 0 WARN），`verify-note.sh` 全部通过。
   - `task/WEB-02-readonly` ff-only 合入 `main`，已合分支已清理。

## Commits
- `cfe3553` [WEB-02] 认领:分支+Board+Dashboard三处同步+ADR双路审核修订
- `2e53414` [WEB-02] 只读大盘与黑匣子录波实现+脱敏及遥测TDD全覆盖
- `03a2cd0` [WEB-02] 完工归档: 只读大盘与黑匣子录波上线+ADR入册
- `2f02673` [WEB-02] 归档卡补Merge注记

## Files Changed
- `.agents/notes/implemented/feature/2026-09-06-web-observability-dashboard-recorder.md`
- `.agents/tasks/web-console/00_DASHBOARD.md`
- `.agents/tasks/web-console/01_TASK_BOARD.md`
- `.agents/tasks/web-console/02_REVIEWS/WEB-02.md`
- `.agents/tasks/web-console/04_ARCHIVED/2026-09-07-WEB-02-readonly.md`
- `web/package.json`
- `web/pnpm-lock.yaml`
- `web/vite.config.ts`
- `web/src/router.ts`
- `web/src/router.guard.test.ts`
- `web/src/views/DashboardStub.vue` (删除)
- `web/src/views/DashboardView.vue`
- `web/src/views/RecorderView.vue`
- `web/src/views/views.flow.test.ts`
- `web/src/types/telemetry.ts`
- `web/src/utils/scrub.ts`
- `web/src/utils/scrub.test.ts`
- `web/src/composables/useTelemetry.ts`
- `web/src/composables/useTelemetry.test.ts`
- `web/src/components/NavBar.vue`
- `web/src/components/StatusBanner.vue`
- `web/src/components/MetricCards.vue`
- `web/src/components/TrendCharts.vue`
- `web/src/components/ProviderMatrix.vue`
- `web/src/components/FrameDrawer.vue`

## Next
- 进入 M4 阶段：`WEB-06 Admin写路径与治理`（写前备份、If-Match 乐观锁、写队列、CUD 操作及拨测接入）。
